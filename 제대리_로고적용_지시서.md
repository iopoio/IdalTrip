# 로고 적용 지시서 — 제대리용

작성: 클과장 / 2026-04-15  
⚠️ 작업 전 CLAUDE.md + 역할-제대리.md 읽을 것.

---

## 배경

- Header가 `hidden` 클래스로 완전히 숨겨진 상태. 로고 보이게 해야 함.
- 푸터의 "IdalTrip" 텍스트도 로고 이미지로 교체.
- 로고 파일: `src/assets/logo/이달여행.svg` (이미 존재)

---

## Fix 1 — `src/components/Header.tsx` 전체 교체

모바일 앱(max-w-430px) 구조에 맞는 심플 탑바로 교체.  
기존 navLinks, 로그인 버튼, 시작하기 버튼 전부 제거. 로고만.

```tsx
import { Link } from 'react-router-dom';
import LogoLight from '../assets/logo/이달여행.svg';

const Header = () => {
  return (
    <header className="sticky top-0 w-full z-50 bg-white/90 backdrop-blur-md border-b border-surface-container">
      <div className="flex items-center px-4 h-12">
        <Link to="/" className="hover:opacity-80 transition-opacity">
          <img src={LogoLight} alt="이달의 여행" className="h-6 w-auto" />
        </Link>
      </div>
    </header>
  );
};

export default Header;
```

---

## Fix 2 — `src/pages/HomePage.tsx` 푸터 로고 교체

HomePage.tsx 상단에 import 추가:
```tsx
import LogoLight from '../assets/logo/이달여행.svg';
```

푸터 텍스트 부분 교체:

기존:
```tsx
<div className="text-xl font-black text-slate-300 tracking-tighter">IdalTrip</div>
```

교체:
```tsx
<img src={LogoLight} alt="이달의 여행" className="h-5 w-auto opacity-40" />
```

---

## 검증 기준 (보고 전 필수 체크)

```
Fix 1: 탑바에 로고 이미지 표시됨: ✅/❌
Fix 1: 탑바가 sticky로 스크롤 시 상단 고정됨: ✅/❌
Fix 1: 탑바가 모바일 너비(430px)에 맞게 표시됨: ✅/❌
Fix 2: 푸터에 로고 이미지 표시됨 (텍스트 대신): ✅/❌
npm run build 통과: ✅/❌
```

---

## 절대 규칙

- .env 수정 금지
- 로고 파일(이달여행.svg, 이달여행_D.svg) 수정 금지
- BottomNav 건드리지 말 것
- 빌드 통과 확인 후 보고
- 검증 기준 ✅/❌ 빠짐없이 보고할 것
