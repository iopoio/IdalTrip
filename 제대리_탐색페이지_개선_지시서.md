# 탐색 페이지 개선 지시서 — 제대리용

작성: 클과장 / 2026-04-15  
⚠️ 작업 전 CLAUDE.md + 역할-제대리.md 읽을 것.

---

## 배경

ExploreResultPage 두 가지 문제:
1. 탭별 데이터 없을 때 empty state 없음 → 흰 화면
2. 출발지 입력이 하단 고정 영역에 있어서 배치 어색 + 키보드 묻힘 + 확인 불가

---

## Fix 1 — 출발지 상단 이동

### 현재 구조 (잘못됨)
```
헤더 → 탭 → 스팟 목록 → [fixed 하단: 출발지 + 자가용 + AI버튼]
```

### 변경 구조
```
헤더 → 출발지 + 자가용 → 탭 → 스팟 목록 → [fixed 하단: AI버튼만]
```

### 헤더 아래에 출발지 섹션 추가

기존 헤더(`<div className="px-4 pt-6 pb-4">`) 바로 아래, 탭 바로 위에 삽입:

```tsx
{/* 출발지 + 교통수단 */}
<div className="flex gap-2 px-4 pb-4">
  <input
    type="text"
    placeholder="출발지 (예: 서울역)"
    value={origin}
    onChange={e => setOrigin(e.target.value)}
    className="flex-1 bg-surface-container-low rounded-xl px-4 py-2.5 text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-primary/30"
  />
  <button
    onClick={() => setTransport(t => t === 'car' ? 'public' : 'car')}
    className="px-4 py-2.5 bg-surface-container rounded-xl text-sm font-bold text-slate-600 transition-colors hover:bg-slate-200 flex-shrink-0"
  >
    {transport === 'car' ? '🚗 자가용' : '🚌 대중교통'}
  </button>
</div>
```

### 하단 고정 영역에서 출발지/교통수단 제거

기존 하단 CTA 블록에서 출발지 input + 교통수단 버튼 div 제거. AI 코스 버튼만 남김:

```tsx
<div className="fixed bottom-0 left-0 right-0 max-w-[430px] mx-auto bg-white/95 backdrop-blur-sm border-t border-surface-container px-4 pt-4 pb-8 z-40">
  <button
    onClick={handleGenerateCourse}
    disabled={selectedIds.length === 0 || generating}
    className="w-full bg-primary-container text-white py-4 rounded-xl font-bold text-base shadow-lg active:scale-[0.98] transition-all disabled:opacity-50 flex items-center justify-center gap-2"
  >
    {generating ? '코스 생성 중...' : `선택한 ${selectedIds.length}곳으로 AI 코스 짜기`}
  </button>
</div>
```

---

## Fix 2 — 탭별 Empty State 추가

스팟 목록 렌더링 부분에서 `filteredSpots`가 비어있을 때 탭별 맞춤 문구 표시.

기존 스팟 목록 렌더링 블록에서:

```tsx
) : (
  <div className="flex flex-col gap-3 px-4">
    {filteredSpots.map(spot => { ... })}
  </div>
)}
```

아래처럼 교체:

```tsx
) : filteredSpots.length === 0 ? (
  <div className="flex flex-col items-center justify-center py-20 px-8 text-center gap-3">
    <span className="text-5xl">
      {activeTab === '축제' ? '🎪' :
       activeTab === '관광지' ? '🏞️' :
       activeTab === '맛집' ? '🍽️' : '🗺️'}
    </span>
    <p className="text-base font-bold text-on-surface">
      {activeTab === '축제' ? '이 날짜에 진행 중인 축제가 없어요' :
       activeTab === '관광지' ? '등록된 관광지 정보가 없습니다' :
       activeTab === '맛집' ? '등록된 맛집 정보가 없습니다' :
       '이 날짜에 해당 지역 정보가 없습니다'}
    </p>
    <p className="text-sm text-slate-400">
      {activeTab === '축제' ? '다른 날짜나 지역을 탐색해보세요' :
       '다른 지역을 선택하거나 홈으로 돌아가세요'}
    </p>
    <button
      onClick={() => navigate('/')}
      className="mt-2 px-6 py-2.5 bg-surface-container rounded-xl text-sm font-bold text-slate-600"
    >
      홈으로
    </button>
  </div>
) : (
  <div className="flex flex-col gap-3 px-4">
    {filteredSpots.map(spot => { ... })}
  </div>
)}
```

`{filteredSpots.map(spot => { ... })}` 부분은 기존 코드 그대로 유지.

---

## 검증 기준 (보고 전 필수 체크)

```
Fix 1: 출발지 입력이 헤더 아래, 탭 위에 위치: ✅/❌
Fix 1: 하단 고정 영역에 AI 코스 버튼만 남음: ✅/❌
Fix 1: 출발지 입력 후 내용이 유지되어 보임: ✅/❌
Fix 2: 축제 탭 데이터 없을 때 🎪 + 문구 표시: ✅/❌
Fix 2: 관광지 탭 데이터 없을 때 🏞️ + 문구 표시: ✅/❌
Fix 2: 맛집 탭 데이터 없을 때 🍽️ + 문구 표시: ✅/❌
Fix 2: 전체 탭 데이터 없을 때 🗺️ + 문구 표시: ✅/❌
Fix 2: 홈으로 버튼 작동: ✅/❌
npm run build 통과: ✅/❌
```

---

## 절대 규칙

- .env 수정 금지
- 스팟 목록 카드 UI 건드리지 말 것
- handleGenerateCourse 로직 건드리지 말 것
- 빌드 통과 확인 후 보고
- 검증 기준 ✅/❌ 빠짐없이 보고할 것
