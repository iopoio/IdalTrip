# 축제소식 페이지 지시서 — 제대리용

작성: 클과장 / 2026-04-15  
⚠️ 작업 전 CLAUDE.md + 역할-제대리.md 읽을 것.

---

## 배경

홈 단순화로 인해 월별 축제 브라우징 기능이 홈에서 빠졌다.  
이 기능을 `/festivals` 독립 페이지로 분리한다.  
코드 대부분 홈에서 재활용. 새로 짜는 코드 최소화.

---

## 수정 1 — `src/pages/FestivalsPage.tsx` 신규 생성

홈에서 월별 축제 목록 로직을 그대로 가져와서 독립 페이지로 만든다.

```tsx
import { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { tourApi } from '../services/tourApi';
import type { Festival } from '../types';
import MonthFilter from '../components/MonthFilter';
import FestivalCard from '../components/FestivalCard';

const FestivalsPage = () => {
  const navigate = useNavigate();
  const [festivals, setFestivals] = useState<Festival[]>([]);
  const [currentMonth, setCurrentMonth] = useState<number>(new Date().getMonth() + 1);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const loadFestivals = async () => {
      setLoading(true);
      const data = await tourApi.fetchFestivals(currentMonth.toString());
      setFestivals(data);
      setLoading(false);
    };
    loadFestivals();
  }, [currentMonth]);

  return (
    <div className="bg-surface text-on-surface pb-24">
      {/* 페이지 타이틀 */}
      <section className="px-6 pt-6 pb-4 bg-white">
        <h1 className="font-headline text-2xl font-bold text-on-surface">축제 소식</h1>
        <p className="text-sm text-slate-500 mt-1">전국 각지의 생생한 축제 소식을 전해드립니다.</p>
      </section>

      {/* 월 필터 */}
      <section className="pt-4 pb-6 bg-surface-container-low overflow-visible">
        <div className="px-6 overflow-visible">
          <MonthFilter currentMonth={currentMonth} onMonthChange={setCurrentMonth} />
        </div>
      </section>

      {/* 축제 그리드 */}
      <section className="px-4 py-6 bg-surface">
        {loading ? (
          <div className="grid grid-cols-2 gap-3">
            {[1, 2, 3, 4, 5, 6].map(i => (
              <div key={i} className="animate-pulse bg-surface-container-high rounded-xl aspect-[3/4]" />
            ))}
          </div>
        ) : festivals.length === 0 ? (
          <div className="py-16 text-center">
            <p className="text-slate-400 text-sm">이 달에는 축제 정보가 없습니다.</p>
          </div>
        ) : (
          <div className="grid grid-cols-2 gap-3">
            {festivals.map((festival) => (
              <FestivalCard
                key={festival.contentid}
                festival={festival}
                onClick={() => navigate(`/festival/${festival.contentid}`)}
              />
            ))}
          </div>
        )}
      </section>
    </div>
  );
};

export default FestivalsPage;
```

---

## 수정 2 — `src/App.tsx`

FestivalsPage import 추가 + 라우트 교체.

```tsx
import FestivalsPage from './pages/FestivalsPage';
```

기존:
```tsx
<Route path="/festivals" element={<ComingSoon title="축제 소식" />} />
```
교체:
```tsx
<Route path="/festivals" element={<FestivalsPage />} />
```

ComingSoon 컴포넌트는 `/course-gen`, `/mypage`에서 아직 사용 중이므로 삭제 금지.

---

## 수정 3 — `src/components/BottomNav.tsx`

홈 + 축제소식 2탭으로 구성.

```tsx
import React from 'react';
import { Link, useLocation } from 'react-router-dom';
import { Home, Calendar } from 'lucide-react';

const BottomNav: React.FC = () => {
  const location = useLocation();

  const navItems = [
    { name: '홈', path: '/', icon: Home },
    { name: '축제 소식', path: '/festivals', icon: Calendar },
  ];

  return (
    <nav className="fixed bottom-0 w-full max-w-[430px] bg-white/90 backdrop-blur-xl border-t border-surface-container z-50 flex items-center justify-around px-4 pt-3 pb-6">
      {navItems.map((item) => {
        const isActive = location.pathname === item.path;
        const Icon = item.icon;
        return (
          <Link
            key={item.path}
            to={item.path}
            className={`flex flex-col items-center gap-1 transition-all duration-200 ${
              isActive ? 'text-primary' : 'text-on-surface-variant'
            }`}
          >
            <div className={`p-1.5 rounded-xl transition-all ${isActive ? 'bg-primary/10 shadow-sm' : ''}`}>
              <Icon size={22} strokeWidth={isActive ? 2.5 : 2} />
            </div>
            <span className={`text-[10px] font-bold ${isActive ? 'opacity-100' : 'opacity-60'}`}>
              {item.name}
            </span>
          </Link>
        );
      })}
    </nav>
  );
};

export default BottomNav;
```

---

## 검증 기준 (보고 전 필수 체크)

```
/festivals 진입 시 축제소식 페이지 정상 로드: ✅/❌
월 탭 현재 달부터 보임: ✅/❌
축제 카드 클릭 시 상세 페이지 이동: ✅/❌
빈 달 선택 시 "이 달에는 축제 정보가 없습니다" 표시: ✅/❌
하단 탭바 홈/축제소식 2탭 표시: ✅/❌
탭 전환 시 active 스타일 정상 작동: ✅/❌
npm run build 통과: ✅/❌
```

---

## 절대 규칙

- .env 수정 금지
- ExploreResultPage, CourseResultPage, FestivalDetailPage 건드리지 말 것
- ComingSoon 컴포넌트 삭제 금지
- 빌드 통과 확인 후 보고
